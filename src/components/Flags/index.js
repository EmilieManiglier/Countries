import React from 'react';
import Flag from './Flag';

import './flags.scss';

const Flags = () => (
  <main className="main">
    <Flag />
    <Flag />
    <Flag />
    <Flag />
  </main>
);

export default Flags;
